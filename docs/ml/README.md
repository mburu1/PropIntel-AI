# Machine Learning

Predictive features must define a target, training-data lineage, evaluation split, drift signal, confidence threshold, fallback behavior, and human review path before production use. Model quality and groundedness are part of the [`../testing-strategy/`](../testing-strategy/).
