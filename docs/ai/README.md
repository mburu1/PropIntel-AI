# AI

PropIntel AI capabilities are advisory and grounded: document extraction, semantic retrieval, portfolio insights, anomaly detection, forecasting, and operator copilots must expose evidence, confidence, model/version metadata, and approval state.

Security and privacy constraints are defined in [`../security-model/`](../security-model/); AI evaluation requirements are in [`../testing-strategy/`](../testing-strategy/).
